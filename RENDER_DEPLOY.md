# 🚀 تعليمات الرفع على Render.com

لقد قمت بتجهيز المشروع ليعمل بالكامل (الواجهة + الخادم + قاعدة البيانات) على منصة Render.

## 📋 الخطوات:

### 1. إنشاء حساب
قم بإنشاء حساب على [Render.com](https://render.com) واربطه بحساب GitHub الخاص بك.

### 2. إنشاء خدمة جديدة (New Web Service)
- اختر **Web Service**.
- اختر مستودع المشروع (Repository) الخاص بك.

### 3. إعدادات الخدمة (Settings)
- **Name:** اختر اسماً لمشروعك.
- **Environment:** اختر `Node`.
- **Build Command:** `npm run build`
- **Start Command:** `npm run start`

### 4. حفظ البيانات (الخطوة الأهم لـ SQLite)
لكي لا تضيع بياناتك عند كل تحديث، يجب إضافة "Disk" لحفظ قاعدة البيانات:
- اذهب إلى تبويب **Disks** في لوحة تحكم الخدمة على Render.
- اضغط **Add Disk**.
- **Name:** `cms-data`
- **Mount Path:** `/opt/render/project/src/data` (أو ببساطة `./data`)
- **Size:** `1GB` (كافية جداً).

### 5. تهيئة قاعدة البيانات لأول مرة
بعد أن يعمل الموقع، اذهب إلى تبويب **Shell** في Render وقم بتشغيل الأمر التالي لمرة واحدة فقط لإنشاء الجداول والمستخدم الافتراضي:
```bash
npm run init-db
```

---

## 🔑 بيانات الدخول الافتراضية:
- **البريد:** `admin@cms.com`
- **كلمة المرور:** `admin123`

سيقوم الرابط الذي يعطيك إياه Render بفتح الواجهة الأمامية مباشرة، وستتصل بالخادم تلقائياً.
