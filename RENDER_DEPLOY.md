# 🚀 دليل رفع وتشغيل الـ CMS على Render.com

لقد قمت بتجهيز المشروع ليعمل بشكل آلي بالكامل باستخدام خاصية **Render Blueprints**. اتبع هذه الخطوات لإتمام "الربط" وتشغيل الموقع بنجاح:

## 1. رفع التعديلات الأخيرة إلى GitHub
بما أنني قمت بإضافة ملفات جديدة (`render.yaml` و `.gitignore`)، يجب عليك أولاً مزامنة المجلد المحلي مع مستودع GitHub الخاص بك:
```bash
git add .
git commit -m "إضافة إعدادات الرفع الآلي لـ Render"
git push origin main
```

## 2. إتمام الربط على Render (طريقة الـ Blueprint)
هذه أسرع طريقة لضمان إعداد الهاردسك (Disk) والمتغيرات بشكل صحيح:
1. اذهب إلى [لوحة تحكم Render](https://dashboard.render.com).
2. اضغط على زر **New +** ثم اختر **Blueprint**.
3. اختر مستودع المشروع الخاص بك (`cms`).
4. سيتعرف Render تلقائياً على ملف `render.yaml` الذي أنشأته لك.
5. اضغط على **Approve**.

*ملاحظة: الـ Blueprints في Render التي تستخدم "Disk" تتطلب خطة **Starter** (مدفوعة بـ 7$ شهرياً تقريباً). إذا كنت تريد الخطة المجانية، يجب حذف قسم الـ `disk` من ملف `render.yaml` ولكن البيانات ستُحذف عند كل إعادة تشغيل.*

## 3. إعداد قاعدة البيانات لأول مرة
بعد أن تظهر حالة الموقع **Live**، يجب تشغيل الجداول:
1. من لوحة تحكم الخدمة في Render، اذهب إلى تبويب **Shell**.
2. اكتب الأمر التالي:
   ```bash
   npm run init-db
   ```

## 4. حل مشكلة "فشل الاتصال" (Troubleshooting)
إذا ظهرت لك رسالة خطأ في الاتصال، تأكد من الآتي:
- **المخطط الزمني:** انتظر دقيقة بعد تشغيل `init-db` ليتعرف النظام على الملف الجديد.
- **رابط الـ API:** الموقع مهيأ ليتصل بالـ API تلقائياً عبر الرابط نفسه (`/api`). لا داعي لتغيير أي كود.

---

### 🔑 بيانات الدخول:
- **مدير النظام:** `admin@cms.com` | `admin123`
- **المحرر:** `editor@cms.com` | `editor123`

---
**إذا واجهت أي خطأ في الـ Console أثناء البناء (Build)، انسخ الخطأ هنا وسأقوم بإصلاحه فوراً.**
