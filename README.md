# 📝 نظام إدارة المحتوى (CMS)

نظام إدارة محتوى متكامل وحديث مبني بتقنيات حديثة وتصميم جذاب.

![CMS Dashboard](https://via.placeholder.com/800x400/1a1a2e/6366f1?text=CMS+Dashboard)

## 🚀 المميزات

### الوظائف الأساسية
- ✅ **نظام تسجيل دخول/خروج** آمن باستخدام JWT
- ✅ **نظام صلاحيات** متعدد المستويات (Admin - Editor - User)
- ✅ **إدارة المستخدمين** (إضافة، تعديل، حذف)
- ✅ **إدارة المحتوى** (إنشاء، تعديل، حذف، نشر المقالات)
- ✅ **إدارة التصنيفات** للمحتوى
- ✅ **لوحة تحكم** تعرض الإحصائيات والنشاطات
- ✅ **إعدادات النظام** قابلة للتخصيص
- ✅ **سجل النشاطات** لمتابعة العمليات

### التصميم
- 🎨 تصميم عصري وداكن (Dark Theme)
- 📱 متجاوب مع جميع الأجهزة
- ✨ تأثيرات حركية سلسة
- 🌐 دعم كامل للغة العربية (RTL)

---

## 🛠️ التقنيات المستخدمة

### الواجهة الأمامية (Frontend)
| التقنية | الوصف |
|---------|-------|
| React 18 | مكتبة JavaScript لبناء واجهات المستخدم |
| Vite | أداة بناء سريعة وحديثة |
| React Router | إدارة التنقل |
| Axios | مكتبة لطلبات HTTP |
| Lucide Icons | مكتبة أيقونات |
| React Hot Toast | إشعارات تفاعلية |

### النظام الخلفي (Backend)
| التقنية | الوصف |
|---------|-------|
| Node.js | بيئة تشغيل JavaScript |
| Express.js | إطار عمل للخادم |
| SQLite | قاعدة بيانات خفيفة |
| better-sqlite3 | مكتبة SQLite سريعة |
| JWT | نظام المصادقة |
| bcryptjs | تشفير كلمات المرور |

---

## 📁 هيكل المشروع

```
cms/
├── 📁 client/                    # الواجهة الأمامية
│   ├── 📁 src/
│   │   ├── 📁 components/        # المكونات المشتركة
│   │   │   ├── Header.jsx
│   │   │   ├── Header.css
│   │   │   ├── Sidebar.jsx
│   │   │   ├── Sidebar.css
│   │   │   ├── Layout.jsx
│   │   │   └── Layout.css
│   │   ├── 📁 context/           # حالة التطبيق
│   │   │   └── AuthContext.jsx
│   │   ├── 📁 pages/             # صفحات التطبيق
│   │   │   ├── Login.jsx
│   │   │   ├── Dashboard.jsx
│   │   │   ├── Articles.jsx
│   │   │   ├── ArticleEditor.jsx
│   │   │   ├── Categories.jsx
│   │   │   ├── Users.jsx
│   │   │   ├── Settings.jsx
│   │   │   └── Profile.jsx
│   │   ├── 📁 utils/             # أدوات مساعدة
│   │   │   └── api.js
│   │   ├── App.jsx
│   │   ├── main.jsx
│   │   └── index.css
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── 📁 server/                    # النظام الخلفي
│   ├── 📁 config/                # إعدادات
│   │   ├── database.js
│   │   └── auth.js
│   ├── 📁 middleware/            # الوسيط
│   │   └── auth.js
│   ├── 📁 routes/                # المسارات
│   │   ├── auth.js
│   │   ├── users.js
│   │   ├── articles.js
│   │   ├── categories.js
│   │   └── dashboard.js
│   ├── index.js
│   └── initDb.js
│
├── 📁 data/                      # قاعدة البيانات (تُنشأ تلقائياً)
├── 📁 uploads/                   # الملفات المرفوعة
├── package.json
└── README.md
```

---

## 🗄️ تصميم قاعدة البيانات

### جدول المستخدمين (users)
| العمود | النوع | الوصف |
|--------|------|-------|
| id | INTEGER | المعرف الفريد |
| username | TEXT | اسم المستخدم |
| email | TEXT | البريد الإلكتروني |
| password | TEXT | كلمة المرور (مشفرة) |
| role | TEXT | الدور (admin/editor/user) |
| avatar | TEXT | صورة الملف الشخصي |
| status | TEXT | الحالة (active/inactive) |
| created_at | DATETIME | تاريخ الإنشاء |
| updated_at | DATETIME | تاريخ التحديث |

### جدول المقالات (articles)
| العمود | النوع | الوصف |
|--------|------|-------|
| id | INTEGER | المعرف الفريد |
| title | TEXT | العنوان |
| slug | TEXT | الرابط المختصر |
| content | TEXT | المحتوى |
| excerpt | TEXT | المقتطف |
| featured_image | TEXT | الصورة الرئيسية |
| status | TEXT | الحالة (draft/published/archived) |
| category_id | INTEGER | معرف التصنيف |
| author_id | INTEGER | معرف الكاتب |
| views | INTEGER | عدد المشاهدات |
| created_at | DATETIME | تاريخ الإنشاء |
| updated_at | DATETIME | تاريخ التحديث |
| published_at | DATETIME | تاريخ النشر |

### جدول التصنيفات (categories)
| العمود | النوع | الوصف |
|--------|------|-------|
| id | INTEGER | المعرف الفريد |
| name | TEXT | اسم التصنيف |
| slug | TEXT | الرابط المختصر |
| description | TEXT | الوصف |
| created_at | DATETIME | تاريخ الإنشاء |

### جدول سجل النشاطات (activity_log)
| العمود | النوع | الوصف |
|--------|------|-------|
| id | INTEGER | المعرف الفريد |
| user_id | INTEGER | معرف المستخدم |
| action | TEXT | نوع العملية |
| entity_type | TEXT | نوع الكيان |
| entity_id | INTEGER | معرف الكيان |
| details | TEXT | تفاصيل إضافية |
| ip_address | TEXT | عنوان IP |
| created_at | DATETIME | التاريخ |

---

## 🔌 واجهات API

### المصادقة (Authentication)
```
POST   /api/auth/login     - تسجيل الدخول
POST   /api/auth/register  - إنشاء حساب جديد
GET    /api/auth/me        - الحصول على بيانات المستخدم الحالي
PUT    /api/auth/profile   - تحديث الملف الشخصي
POST   /api/auth/logout    - تسجيل الخروج
```

### المستخدمين (Users) - للمدير فقط
```
GET    /api/users          - قائمة المستخدمين
GET    /api/users/:id      - مستخدم واحد
POST   /api/users          - إنشاء مستخدم
PUT    /api/users/:id      - تحديث مستخدم
DELETE /api/users/:id      - حذف مستخدم
```

### المقالات (Articles)
```
GET    /api/articles       - قائمة المقالات
GET    /api/articles/:id   - مقال واحد
POST   /api/articles       - إنشاء مقال (محرر/مدير)
PUT    /api/articles/:id   - تحديث مقال
DELETE /api/articles/:id   - حذف مقال
```

### التصنيفات (Categories)
```
GET    /api/categories     - قائمة التصنيفات
GET    /api/categories/:id - تصنيف واحد
POST   /api/categories     - إنشاء تصنيف (مدير)
PUT    /api/categories/:id - تحديث تصنيف
DELETE /api/categories/:id - حذف تصنيف
```

### لوحة التحكم (Dashboard)
```
GET    /api/dashboard/stats      - الإحصائيات
GET    /api/dashboard/activities - سجل النشاطات
GET    /api/dashboard/settings   - الإعدادات
PUT    /api/dashboard/settings   - تحديث الإعدادات
```

---

## ⚙️ خطوات التشغيل

### المتطلبات المسبقة
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

### 1️⃣ تثبيت الحزم
```bash
# في مجلد المشروع الرئيسي
npm install

# في مجلد الواجهة الأمامية
cd client
npm install
cd ..
```

### 2️⃣ إنشاء قاعدة البيانات
```bash
npm run init-db
```

هذا الأمر سيُنشئ:
- قاعدة بيانات SQLite
- الجداول المطلوبة
- مستخدم مدير افتراضي
- بيانات تجريبية

### 3️⃣ تشغيل المشروع

**تشغيل الخادم فقط:**
```bash
npm run server
```

**تشغيل الواجهة فقط:**
```bash
npm run client
```

**تشغيل الكل معاً:**
```bash
npm run dev
```

### 4️⃣ الوصول للتطبيق
- الواجهة الأمامية: http://localhost:3000
- الخادم (API): http://localhost:5000

---

## 🔑 بيانات الدخول الافتراضية

| الدور | البريد الإلكتروني | كلمة المرور |
|-------|------------------|-------------|
| مدير | admin@cms.com | admin123 |
| محرر | editor@cms.com | editor123 |

---

## 🔒 الصلاحيات

| الصلاحية | مدير | محرر | مستخدم |
|----------|------|------|--------|
| عرض لوحة التحكم | ✅ | ✅ | ✅ |
| إدارة المقالات | ✅ | ✅ | ❌ |
| إدارة التصنيفات | ✅ | ✅ | ❌ |
| إدارة المستخدمين | ✅ | ❌ | ❌ |
| الإعدادات | ✅ | ❌ | ❌ |
| سجل النشاطات | ✅ | ❌ | ❌ |

---

## 🎯 الخطوات القادمة (للتطوير المستقبلي)

- [ ] إضافة نظام رفع الملفات والصور
- [ ] إضافة محرر نصوص غني (WYSIWYG)
- [ ] إضافة نظام التعليقات
- [ ] إضافة البحث المتقدم
- [ ] إضافة التصدير والاستيراد
- [ ] إضافة نظام الإشعارات
- [ ] إضافة دعم تعدد اللغات
- [ ] إضافة وضع الصيانة الفعلي

---

## 📝 ملاحظات

- تأكد من تثبيت Node.js قبل البدء
- قاعدة البيانات SQLite لا تحتاج إلى تثبيت خادم منفصل
- ملف قاعدة البيانات سيتم إنشاؤه في مجلد `data/`
- يمكنك تغيير مفتاح JWT في ملف `server/config/auth.js`

---

## 📞 الدعم

إذا واجهت أي مشاكل، تأكد من:
1. تثبيت جميع الحزم بنجاح
2. تشغيل `npm run init-db` قبل البدء
3. التأكد من عدم استخدام المنافذ 3000 و 5000 من تطبيقات أخرى

---

**تم التطوير بـ ❤️ باستخدام React + Node.js**
